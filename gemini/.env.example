# Gemini MCP Server Configuration

# Enable/disable Gemini integration
GEMINI_ENABLED=true

# Enable/disable automatic consultation on uncertainty detection
GEMINI_AUTO_CONSULT=true

# Command to execute Gemini CLI (default: gemini)
GEMINI_CLI_COMMAND=gemini

# Timeout for Gemini queries in seconds (default: 60)
GEMINI_TIMEOUT=60

# Rate limit delay between API calls in seconds (default: 2)
GEMINI_RATE_LIMIT=2

# Gemini model to use (default: gemini-2.0-flash-exp)
GEMINI_MODEL=gemini-2.0-flash-exp

# Maximum context length for queries (optional)
# GEMINI_MAX_CONTEXT=8000

# Enable debug logging (default: false)
# GEMINI_DEBUG=false